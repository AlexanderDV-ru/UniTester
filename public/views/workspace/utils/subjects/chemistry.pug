extends /headered

block title
	title #{$msgs$.sitename} - #{$msgs$.chemistryUtils} - #{$msgs$.sitename}

block inHead0
	link(rel='stylesheet', href='/stylesheets/style2.css')

block content
	include /menues